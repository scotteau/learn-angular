import { DoubleDirective } from './double.directive';

describe('DoubleDirective', () => {
  it('should create an instance', () => {
    const directive = new DoubleDirective();
    expect(directive).toBeTruthy();
  });
});
